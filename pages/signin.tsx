import Head from "next/head";
import React, { ReactElement } from "react";

const Signin = () => {
  return (
    <div>
      <Head>
        <title>Signin</title>
      </Head>
      <div>sign in</div>
    </div>
  );
};

export default Signin;
